import React from 'react';
import transition from "../../components/transition";
import Header from "../../components/Header/Header";

const Board = () => {
    return (
        <div className="frame">
            <>
            </>
        </div>
    );
};

export default transition(Board);